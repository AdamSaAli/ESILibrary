import '../app/globals.css'; // Adjust the path if your globals.css file is in a different folder

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;